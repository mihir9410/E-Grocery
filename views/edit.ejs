<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" crossorigin="anonymous">
    <title>Edit Entry</title>
    <style>
        .highlight {
            background: #dfedfb;
            padding: 0;
            text-align: center;
            margin: 15px 0;
        }

        .highlight .heading {
            border-bottom: solid 2px #0d6efd;
            text-align: left;
            margin-bottom: 25px;
            padding-bottom: 15px;
            background: #0d6efd;
            color: #fff;
            font-size: 19px;
            font-weight: 600;
            padding: 15px 15px;
        }

        .highlight form {
            padding: 15px 25px;
        }
    </style>
</head>
<body>
    <div class="container w-50">
        <div class="highlight">
            <div class="heading">Edit Entry</div>
            <form method="post" action="/update/<%= data._id %>" enctype="multipart/form-data">
                <div class="mb-3">
                    <label for="name" class="form-label">Name</label>
                    <input type="text" name="name" class="form-control" value="<%= data.name %>" required>
                </div>
                <div class="mb-3">
                    <label for="quantity" class="form-label">Quantity</label>
                    <input type="number" name="quantity" class="form-control" value="<%= data.quantity %>" required>
                </div>
                <div class="mb-3">
                    <label for="date" class="form-label">Date</label>
                    <input type="date" name="date" class="form-control" value="<%= data.date.toISOString().split('T')[0] %>" required>
                </div>
                <div class="mb-3">
                    <label for="picture" class="form-label">Image</label>
                    <input type="file" name="picture" class="form-control">
                    <img src="/images/<%= data.picture %>" alt="Image" width="100" class="mt-3">
                </div>
                <button type="submit" class="btn btn-primary">Update</button>
            </form>
        </div>
    </div>
</body>
</html>
